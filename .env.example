# =============================================================================
# CW_Budget — Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# NEVER commit your .env file to version control.
# =============================================================================

# ---------------------------------------------------------------------------
# Database (Required)
# ---------------------------------------------------------------------------
# For TiDB Serverless, find these in your TiDB Cloud console under
# "Connect" → "General" → connection parameters.
DB_HOST=your_host
DB_PORT=your_port
DB_NAME=your_database_name
DB_USER=your_username
DB_PASSWORD=your_password

# ---------------------------------------------------------------------------
# Flask (Optional)
# ---------------------------------------------------------------------------
# Secret key for session signing. Generate a random one for production:
#   python -c "import secrets; print(secrets.token_hex(32))"
# SECRET_KEY=change-me-to-a-random-string
