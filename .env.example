# =============================================================================
# CW_Budget — Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# NEVER commit your .env file to version control.
# =============================================================================

# ---------------------------------------------------------------------------
# Database (Required)
# ---------------------------------------------------------------------------
# For TiDB Serverless, find these in your TiDB Cloud console under
# "Connect" → "General" → connection parameters.
DB_HOST=6oaaat.h.filess.io
DB_PORT=3307
DB_NAME=your_database_name
DB_USER=your_username
DB_PASSWORD=your_password

# ---------------------------------------------------------------------------
# Database Pool (Optional)
# ---------------------------------------------------------------------------
# TiDB Serverless free tier allows max 5 concurrent connections per user.
# Default of 4 leaves 1 slot as server-side safety margin.
# Increase this if you upgrade to a paid plan with a higher connection limit.
# DB_POOL_SIZE=4

# ---------------------------------------------------------------------------
# Flask (Optional)
# ---------------------------------------------------------------------------
# Secret key for session signing. Generate a random one for production:
#   python -c "import secrets; print(secrets.token_hex(32))"
# SECRET_KEY=change-me-to-a-random-string
